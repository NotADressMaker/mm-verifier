{
  "name": "@mamv/receipt-verifier",
  "version": "0.1.0",
  "description": "Create and independently verify portable MAMV receipts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test test/*.test.js"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "author": "MAMV Team",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
