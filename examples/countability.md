# Countability example

The set of all finite strings over a finite alphabet is countable. Programs can be encoded as finite strings, so the set of programs is countable. The Halting Problem is undecidable. Decidability implies countable domain.
