version: '3.8'

services:
  # Redis for job queue and caching
  redis:
    image: redis:7-alpine
    container_name: llm-verifier-redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL for API data
  postgres:
    image: postgres:15-alpine
    container_name: llm-verifier-postgres
    environment:
      POSTGRES_DB: llm_verifier
      POSTGRES_USER: verifier
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U verifier']
      interval: 10s
      timeout: 5s
      retries: 5

  # IPFS for evidence storage
  ipfs:
    image: ipfs/kubo:latest
    container_name: llm-verifier-ipfs
    ports:
      - '4001:4001' # P2P
      - '5001:5001' # API
      - '8080:8080' # Gateway
    volumes:
      - ipfs-data:/data/ipfs
    environment:
      IPFS_PROFILE: server

  # API server
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: llm-verifier-api
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      API_PORT: 3000
      REDIS_URL: redis://redis:6379
      DATABASE_URL: postgresql://verifier:${DB_PASSWORD:-changeme}@postgres:5432/llm_verifier
      ARBITRUM_SEPOLIA_RPC_URL: ${ARBITRUM_SEPOLIA_RPC_URL}
      PRIVATE_KEY: ${PRIVATE_KEY}
      MARKETPLACE_ADDRESS: ${MARKETPLACE_ADDRESS}
      STAKING_ADDRESS: ${STAKING_ADDRESS}
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    restart: unless-stopped

  # Verifier node
  verifier-node:
    build:
      context: ./verifier-node
      dockerfile: Dockerfile
    container_name: llm-verifier-node
    environment:
      NODE_ENV: production
      REDIS_URL: redis://redis:6379
      IPFS_API_URL: http://ipfs:5001
      ARBITRUM_SEPOLIA_RPC_URL: ${ARBITRUM_SEPOLIA_RPC_URL}
      VERIFIER_PRIVATE_KEY: ${VERIFIER_PRIVATE_KEY}
      MARKETPLACE_ADDRESS: ${MARKETPLACE_ADDRESS}
      STAKING_ADDRESS: ${STAKING_ADDRESS}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      AUTO_REGISTER_VERIFIER: 'true'
    depends_on:
      redis:
        condition: service_healthy
      ipfs:
        condition: service_started
    restart: unless-stopped

volumes:
  redis-data:
  postgres-data:
  ipfs-data:

networks:
  default:
    name: llm-verifier-network
